name: onyx
version: 0.2.0
description: "Discord command handler based around functioning with Nyxx's Rest implementation."

environment:
  sdk: ">=2.17.0 <3.0.0"

dev_dependencies:
  alfred: ^0.1.7
  cryptography: ^2.0.5
  http: ^0.13.5
